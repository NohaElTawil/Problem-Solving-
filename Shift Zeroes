#include <iostream>
#include "bits/stdc++.h"
using namespace std;
int printCount (int n) {
    int counter = 0 ;
    for (int i = 0 ; i < n ; i++) {
        int a ;
        cin >> a ;
        if (a==0) {
            counter++ ;
        }
        else {
            cout << a << " ";
        }
    }
    return counter ;
}
int main() {
int n ;
cin >> n ;
int zeros = printCount(n) ;
for (int i = 0 ; i < zeros ; i++) {
    cout << 0 << " ";
}
}
