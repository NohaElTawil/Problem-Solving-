#include <iostream>
#include "bits/stdc++.h"
using namespace std;
long long sum (long long x , long long n) {
    long long s = 0, c = 2  ;
    while (c<=n) {
        s+=pow(x,c) ;
        c+=2;
    }
    return s ;
}

int main() {
long long x , n ;
cin >> x >> n ;
cout << sum(x,n) ; 
}
