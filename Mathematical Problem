#include<iostream>
using namespace  std ;
int main () {
    int n;
    cin >> n;

    int result;
    if (n % 2 == 0)
        result = n / 2;
    else
        result = (-1 * (n + 1)) / 2;

    cout << result << endl;

    return 0;
}
