#include <iostream>
using namespace std;

const int s = 10000;

int main() {
    int n, shifts;
    cin >> n >> shifts;

    int arr[s];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int shifts2 = shifts % n;

    if (shifts2 == 0) {
        for (int i = 0; i < n; i++) cout << arr[i] << " ";
    } else {

        for (int i = n - shifts2; i < n; i++) cout << arr[i] << " ";
        
        for (int i = 0; i < n - shifts2; i++) cout << arr[i] << " ";
    }

    cout << endl;
    return 0;
}
